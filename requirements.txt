# Core runtime (rewrite / embed / retrieve / answer pipeline)
openai==1.108.2          # DeepSeek official API via OpenAI-compatible client
numpy==1.26.4
torch==2.8.0             # embedding tensor ops (L2 normalization); CPU build is sufficient
sentence-transformers==3.0.1
requests==2.32.5
regex==2022.4.24
jsonschema==4.25.1       # rewrite / keyword JSON-schema validation
python-dotenv==1.1.1     # load DEEPSEEK_API_KEY / local embedding settings from .env
tqdm==4.67.1
nltk==3.9.1              # EAES lexical stemming and evaluation metrics

# Evaluation only (eval/*.py)
